const { Client, Message, MessageEmbed } = require('discord.js');
const { afk } = require("../../utils/afk")


module.exports = {
    name: 'raddabits',
    category: "music",
    memberpermissions: ["SEND_MESSAGES"],
    description: 'Stats for the song Rad Dabbits',
    options: 
    run: async (client, interaction, args) => {

          'interaction.user.id) return interaction.editReply({ content: `5% of the money from this song goes to charity. It has 173 streams in total. On Youtube it has 161 views. })
       